<?php

/* this is where all our settings (like global directory variables) and function files could be pulled settings. I did this with my site, and it seems to work pretty well, though it does necessarily add a little bloat for some pagest that don't need everything (like most of them), but it does cut down on complexity something to think about */

/* Looks good, let's do it - R*/

$SITE_NAME = 'Basic CMS I';

?>